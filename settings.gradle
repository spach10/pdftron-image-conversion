pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://pdftron-maven.s3.amazonaws.com/release' }
        maven { url 'https://s3.amazonaws.com/tgl.maven' }
    }
}
rootProject.name = "PDF Tron Image Conversion"
include ':app'
